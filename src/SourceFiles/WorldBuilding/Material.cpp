#include "../../HeaderFiles/WorldBuilding/Material.h"

Material::Material(float colorR, float colorG, float colorB, float smoothness, bool lightEmitting) : 
colorR(colorR), colorG(colorG), colorB(colorB), smoothness(smoothness), lightEmitting(lightEmitting) {
    
}
